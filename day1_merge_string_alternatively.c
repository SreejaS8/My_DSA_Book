//merge two strings alternatively
// input : s1 = "abc", s2 = "pqr"
// output : "apbqcr"
#include <stdio.h>

int mergeAlternatively(void){
    char s1,s2,m;
    if (s1>s2){m = s1}
    else{m = s2}
    for(int i=0; i <= len(m); i++){
        
    }
}